---
context: default
phase: n/a — revontuli: MiniMax H3 on RX 6900 XT eGPU (published)
task: 12 of 12 — everything shipped; only optional threads remain
status: paused
last_updated: 2026-08-21T16:42:47Z
---

# CONTINUE HERE — paused 2026-08-21 (fourth pause; first in the new home)

> This workspace was **renamed and moved this session**: `~/minimax-h3-egpu` →
> `~/Sites/revontuli`, now a public repo at **github.com/mindless/revontuli**.
> The h3.c fork is a real GitHub fork: **github.com/mindless/h3.c**, branch
> `intel-amd-egpu` (local remote `fork`; `origin` stays antirez upstream).
> Resume by running `/gsd-resume-work` from this directory — proven to work,
> it is exactly how the previous two sessions resumed.

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered by failing, not by predicting.

- [ ] CONSTRAINT: **Verify output bit-identity after every code change** — the reference
  is the setup prompt at 608x352/22f/4-step seed 42 → md5
  `886b40d10c5a83fb01393a4c62bdfcbd`. It has held through ten perf commits, the
  mps-gpu counter, the SDPA fix, and a CLT→Xcode toolchain switch. If a change moves
  it, the change is wrong until proven otherwise.
- [ ] CONSTRAINT: **A valid MP4 proves nothing.** Every corrupt output exited 0 and
  passed ffprobe. Screen with signalstats mean YAVG (healthy 106–120, corrupt 36–65),
  then extract a frame grid and LOOK at it. Both one-liners are in the README.
- [ ] CONSTRAINT: **Random-data testing cannot expose uniform-attention corruption** —
  broken-uniform SDPA output differs from correct near-uniform softmax by only ~0.007
  on random inputs. Use the planted-spike probe (`src/sdpa_probe.m --spike`): a
  dropped 28-sigma spike fails at err ~1.0 and maps exactly which heads/queries broke.
- [ ] CONSTRAINT: **Never read `root-gpu` as total GPU time** — it misses MPSGraph's
  commitAndContinue-retired roots (~10x understatement; it once retired the matmul
  plan on a false 7% figure). Use `gpu-total = root-gpu + mps-gpu` (patch 0011).
- [ ] CONSTRAINT: **Do not project a win before the terms sum to the phase wall.**
  Violated twice on this workload; both times the "win" was zero.

<current_state>
**Done, published, verified.** MiniMax H3 generates 608x352 video with synchronized
audio on the RX 6900 XT eGPU: 22f/0.92s in 4.0 min (51.4 min originally, 12.7x),
124f/5.175s in 14.2 min. All frame lengths 5..362 work since the SDPA fix.

The headline finding: **Apple MPSGraph native SDPA silently corrupts attention when
its internal scores tensor crosses 2^31 elements** (56 heads: seq 6192 clean / 6193
broken, measured exactly). Fixed with head-chunked SDPA gated at heads*seq^2 > 1.9e9
(fork commit `7d2a930`); below the gate the graph is byte-identical. Full trail:
`logs/51-INVESTIGATION-STATE.md`; reproducer: `src/sdpa_probe.m`.

Repo state: main at `2ff4591`, clean, pushed. Fork tip `7d2a930` (12 commits past
upstream `8974cc0`), pushed. Scripts are self-locating (smoke-verified from this
path, YAVG 116.8). GSD 1.11.0; Node 24.19.0 via `~/.local/opt/node24` (PATH-first
symlinks in `~/.local/bin`; old v22 untouched at `/usr/local/bin/node`).
</current_state>

<remaining_work>
Nothing owed. Three optional threads, owner's call:

1. **Upstream PR to antirez/h3.c** — the broadly-useful subset is device selection
   (0001/0002), the mps-gpu counter (0011), and the SDPA 2^31 guard (0012), not the
   whole eGPU port. GitHub already offered `h3.c/pull/new/intel-amd-egpu`.
2. **Apple Feedback** on the MPSGraph bug — `src/sdpa_probe.m` is the minimal
   reproducer, no model required. Size-gated, not AMD-gated.
3. **Paused perf thread** (owner paused optimizations 2026-08-21; reopen only on
   ask). What's genuinely live if reopened: GPU busy is 71.7–99.9% of the denoise,
   so the matmul/flash-attention work retired on the false 7% figure is back on the
   table; and at 22f there are ~41 s of inter-buffer scheduling gaps (the per-block
   `h3_gpu_submit` drains the pipeline every block). `xctrace` is NOW AVAILABLE
   (Xcode installed + licensed); `bin/trace-denoise.sh` exists but its export/parse
   step has never been executed — verify table names against the TOC it dumps.
</remaining_work>

<human_actions_pending>
- Restart Claude Code (GSD 1.11.0 installed; a running session still has 1.10.0).
- CARRIED, optional: swap the Thunderbolt cable (20 → 40 Gb/s link). The transfer
  path is done, so this no longer gates anything.
</human_actions_pending>

## Infrastructure (updated for the new home)
- **Model**: `~/Sites/revontuli/models/MiniMax-H3` — 134 GiB, FL2VA only, gitignored.
  Do not re-download; `bin/download_model.sh` exists if ever needed.
- **Launcher**: `./generate.sh "PROMPT" W H FRAMES STEPS OUT SEED` — self-locating,
  eGPU-pinned, refuses Intel-UHD fallback. Frames snap to 5+17k, range 5..362.
- **Binary**: `src/h3.c/h3` built with Xcode clang, verified bit-identical. A backup
  of the verified binary is at `/tmp/h3-verified-backup` (volatile — /tmp).
- **Fork remotes** (in `src/h3.c`): `origin` = antirez upstream, `fork` =
  github.com/mindless/h3.c.
- **Reference output**: `outputs/h3-egpu-test.mp4`, md5 `886b40d10c5a83fb01393a4c62bdfcbd`.
- **Env knobs**: table in README.md.
- **Old path `~/minimax-h3-egpu` no longer exists.** Historical logs and this file's
  predecessors reference it; read those paths as `~/Sites/revontuli`.

<context>
The through-line of the last two sessions: every wrong conclusion came from a counter
measured at the wrong boundary — `root-gpu` as a total, concurrent stream-thread time
subtracted from a wall clock, random-data SDPA error looking mild because uniform
attention mimics correct near-uniform softmax. Each was corrected by a *cheaper*
measurement: reading all 535 DiT tensor shapes beat generating (killed two peer
models' top hypothesis for free); a planted spike beat random data; the exact
6192/6193 boundary named the mechanism. Bit-identity is the invariant that makes
everything else trustworthy.
</context>

<next_action>
Nothing owed. To continue work: `claude` in `~/Sites/revontuli`, then
`/gsd-resume-work`. Pick any of the three optional threads above, or generate videos
— the rail is production-ready as-is.
</next_action>
